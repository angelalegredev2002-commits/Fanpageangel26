---
// Import the necessary components from their respective component files
import LoginBtn from '@components/ui/buttons/LoginBtn.astro';
const loginBtnText = Astro.currentLocale === 'es' ? 'Iniciar sesión' : 'Log in';
---

{/* Login Button linked directly to /login */}
<LoginBtn title={loginBtnText} />
