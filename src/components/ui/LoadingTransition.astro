---
import BrandLogo from '@components/BrandLogo.astro';
---

<div
  id="loading-overlay"
  class="fixed inset-0 z-[100] flex flex-col items-center justify-center overflow-hidden bg-white text-neutral-900 transition-all duration-700 ease-in-out dark:bg-neutral-950 dark:text-neutral-100"
>
  <div
    class="relative z-10 flex transform flex-col items-center transition-transform duration-700"
    id="loading-content"
  >
    <div class="mb-8 animate-pulse">
      <BrandLogo class="h-16 w-auto" />
    </div>

    <div class="flex items-center gap-2">
      <div
        class="h-2.5 w-2.5 animate-bounce rounded-full bg-orange-500"
        style="animation-delay: 0s;"
      >
      </div>
      <div
        class="h-2.5 w-2.5 animate-bounce rounded-full bg-orange-500"
        style="animation-delay: 0.2s;"
      >
      </div>
      <div
        class="h-2.5 w-2.5 animate-bounce rounded-full bg-orange-500"
        style="animation-delay: 0.4s;"
      >
      </div>
    </div>
  </div>
</div>

<script is:inline>
  // Hide immediately if already played during this navigation session
  if (window.hasPlayedLoading) {
    const overlay = document.getElementById('loading-overlay');
    if (overlay) overlay.style.display = 'none';
  }
</script>

<script>
  const initLoading = () => {
    const win = window as any;
    const overlay = document.getElementById('loading-overlay');
    const content = document.getElementById('loading-content');

    if (overlay && !win.hasPlayedLoading) {
      setTimeout(() => {
        if (content) {
          content.style.opacity = '0';
          content.style.transform = 'translateY(-20px)';
        }

        setTimeout(() => {
          overlay.style.transform = 'translateY(-100%)';
          overlay.style.opacity = '0';

          setTimeout(() => {
            overlay.style.display = 'none';
            win.hasPlayedLoading = true;
          }, 600);
        }, 200);
      }, 600);
    } else if (overlay) {
      overlay.style.display = 'none';
    }
  };

  initLoading();
  document.addEventListener('astro:page-load', initLoading);
</script>
