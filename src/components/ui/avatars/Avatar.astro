---
import { Image } from 'astro:assets';

const { src, alt } = Astro.props;

interface Props {
  src: string;
  alt: string;
}
---

<Image
  class="inline-block h-8 w-8 rounded-full"
  src={src}
  alt={alt}
  inferSize
  loading={'eager'}
/>
